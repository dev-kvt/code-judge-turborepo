{
  "private": true,
  "scripts": {
    "build": "dotenv -- turbo run build",
    "dx": "docker-compose up -d && dotenv -- turbo run dev",
    "dev": "dotenv -- turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "dotenv-cli": "latest",
    "eslint": "^10.8.0",
    "prettier": "^3.9.6",
    "tsconfig": "*",
    "turbo": "latest"
  },
  "name": "codestreax",
  "packageManager": "yarn@1.22.22",
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
